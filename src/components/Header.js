import React from "react";

const Header = () => (
  <header
    style={{
      display: "flex",
      color: "white",
      backgroundColor: "purple",
      padding: "10px",
      alignItems: "center",
      justifyContent: "center",
    }}
  >
    <h1>Task Tracker</h1>
  </header>
);

export default Header;
